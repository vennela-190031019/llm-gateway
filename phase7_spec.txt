This is the LLM Gateway & Observability Platform project. Phases 1–6 are done and committed (scaffolding, database/auth, provider abstraction, routing/retry/fallback, Redis caching, cost tracking + real /chat/completions endpoint). Read backend/app/models/, app/api/v1/prompts.py, and app/schemas/chat.py before writing anything.

Implement Phase 7: Prompt management.

1. ORM models (app/models/prompt.py): PromptTemplate: id, name (unique, e.g. "customer-support"), description, owner_id (FK→users), created_at. PromptVersion: id, prompt_template_id (FK), version (int, auto-incrementing per template), template_text (the actual prompt with {variable} placeholders), variables (JSON list of variable names the template expects), model (default model to use with this version), temperature (default), is_active (whether this is the version served when no explicit version is requested), created_at. Add an Alembic migration.

2. Repository (app/repositories/prompt_repository.py) — CRUD for templates and versions, plus "get latest active version for a template name."

3. PromptService (app/services/prompt_service.py): create_template(name, description, owner); create_version(template_name, template_text, variables, model, temperature) — auto-increments version number for that template; render(template_name, variables: dict, version: int | None) — fetches the right version (latest active if version is None), validates that all required variables are supplied (raise a clear MissingPromptVariableError listing what's missing), and does simple {variable} substitution into template_text using Python's str.format or a safe equivalent — return the rendered string plus the version's default model/temperature; set_active_version(template_name, version) — marks one version active for a template, deactivating others.

4. Real API endpoints (app/api/v1/prompts.py), replacing the Phase 1 stub: POST /prompts — create a template (any authenticated user, or require_admin your call — pick the more sensible one and note why); POST /prompts/{name}/versions — add a new version to a template; GET /prompts — list templates; GET /prompts/{name} — get a template with its versions; GET /prompts/{name}/render — render a version with query-param or body-supplied variables, without calling any LLM; PATCH /prompts/{name}/versions/{version}/activate — set active version.

5. Don't wire prompt rendering into /chat/completions yet unless it's trivial to do cleanly — if you do wire it, keep it fully backward compatible with the existing raw-messages flow, and call it out clearly in your summary so I know it happened.

6. Tests: cover template/version creation, version auto-increment, render with all variables present, render with missing variables (error case), render with a specific version vs. latest-active, activating a different version changes what "latest active" returns, and API-level tests for each endpoint including auth requirements.

After implementing, run uv pip install -e ".[dev]", ruff check ., mypy app, and pytest -q in backend — fix everything until clean, then summarize what you built, especially your role-permission decision on POST /prompts and whether you wired rendering into /chat/completions, and the final check output. Do not commit — leave it in the working tree for review.
