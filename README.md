# LLM Gateway & Observability Platform

> Work in progress — scaffolding only (Phase 1). Full README with
> architecture diagrams, setup instructions, and screenshots will be
> written in Phase 13.

Production-oriented open-source AI infrastructure project: a unified
LLM gateway with intelligent routing, fallback, caching, cost tracking,
prompt management, evaluation, and observability.

See `docker-compose.yml` for local services and `backend/.env.example`
for required configuration.
